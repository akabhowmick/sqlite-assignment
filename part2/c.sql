-- Below is a dummy query, if we leave it empty then the SQLITE package will throw a non-readable error
-- Change the query to pass the test :)
SELECT  
  *
FROM 
  dogs
WHERE 
  name="DOOMSLAYER";

-- Test File asks to return doomslayer but the part2.md says Zoey